Task
Create a variable named empty and assign it an empty string value. '' Create a variable named emptyLength and assign it a string length value of empty.
Here is an example that is fundamentally similar to what needs to be done, but adapt it to the requirements of the task:
const address = '';
const addressLength = address.length;

Solution
const empty = '';
const emptyLength = '' .length;
